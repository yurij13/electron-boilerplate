// add types here

interface Window {
    electron: {
        // add types here IPCRenderer
    };
}

type EventPayloadMapping = {
    // map of event names to payload types 
    // e.g. "get-cpu-usage": {
    //     payload: {
    //         type: "number"
    //     }
    // } 
}